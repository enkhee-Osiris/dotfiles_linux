# -*- mode: snippet -*-
# name: React Class Component with Flow
# key: rccf
# --
import React, { Component } from 'react';

type Props = {
  ${1:foo: string}
};

const defaultProps = {
  ${2:foo: ''}
};

class ${3:Foo} extends Component<Props> {
  constructor(props: Props) {
    super(props);
  }

  render() {
    return (
      ${0:`%`}
    );
  }
}

$3.defaultProps = defaultProps;

export default $3;
