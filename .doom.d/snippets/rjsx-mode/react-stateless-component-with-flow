# -*- mode: snippet -*-
# name: React Stateless Component with Flow
# key: rscf
# --
import React from 'react';

type Props = {
  ${1:foo: string}
};

const defaultProps = {
  ${2:foo: ''}
};

function ${3:Foo}(props: Props) {
  return (
    ${0:`%`}
  );
}

$3.defaultProps = defaultProps;

export default $3;
